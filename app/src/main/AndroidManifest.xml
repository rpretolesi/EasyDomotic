<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pretolesi.easydomotic" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/android:Theme.Holo"
            android:name=".Runtime.RunTimeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/android:Theme.Holo"
            android:name=".Settings.SettingsActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:windowSoftInputMode="stateHidden|adjustResize"
            android:parentActivityName = ".Control.LightSwitchControlPropActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/android:Theme.Holo"
            android:name=".Control.LightSwitchControlPropActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:windowSoftInputMode="stateHidden|adjustResize"
            android:parentActivityName = ".Control.NumericValueControlPropActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/android:Theme.Holo"
            android:name=".Control.NumericValueControlPropActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:windowSoftInputMode="stateHidden|adjustResize"
            android:parentActivityName = ".Control.SensorValueControlPropActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/android:Theme.Holo"
            android:name=".Control.SensorValueControlPropActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:windowSoftInputMode="stateHidden|adjustResize"
            android:parentActivityName = ".TcpIpClient.TcpIpClientProtocolPropActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/android:Theme.Holo"
            android:name=".TcpIpClient.TcpIpClientProtocolPropActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:windowSoftInputMode="stateHidden|adjustResize"
            android:parentActivityName = ".BluetoothClient.BluetoothClientProtocolPropActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/android:Theme.Holo"
            android:name=".BluetoothClient.BluetoothClientProtocolPropActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:windowSoftInputMode="stateHidden|adjustResize"
            android:parentActivityName = ".BluetoothClient.BluetoothClientConfiguration"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/android:Theme.Holo"
            android:name=".BluetoothClient.BluetoothClientConfiguration"
            android:label="@string/app_name" >
        </activity>

    </application>

</manifest>
